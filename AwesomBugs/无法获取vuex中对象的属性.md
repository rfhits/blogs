在修改了`store/index.js`中某个属性后，`console.log`查看，发现没有被修改。

原因是，前端此时处于运行状态，并且因为个人的设置，页面刷新时，会将vuex中的值保存到本地，完成后，又将值从本地取出，或许覆盖了修改后的值。

所以`npm run serve`跑起来后，可以做到页面的实时渲染，但是vuex或许会因为个人的设置导致没法与`store/index.js`中的内容同步。

解决方法就是停止项目，重新启动。

凌晨三点的bug终于找出来是啥原因了，心累。