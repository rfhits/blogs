1. api的url要落到实体或者联系上。

2. vue的component要遵循开发建议，不要乱放，做好views和components的区分

3. keyword还是searchText命名要统一

4. 关于前后端传输字段，传输的内容要做到面向对象，否则会导致频繁地对字段赋值。

5. 存储的数据尽量都面向对象，尽量避免从url里直接获取数据，否则移植性差。

5. 接上一条，什么时候字段用id，什么时候用entityId？个人感觉在对象内部就可以不用加上前缀。

6. 要时时刷新的值，要写道computed里，不要放到data里。

7. 和后端的开发环境要统一